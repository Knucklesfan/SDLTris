{
    "configurations": [
        {
            "type": "gdb",
            "request": "launch",
            "name": "(gdb) Launch",
            "preLaunchTask": "make",
            "target": "${workspaceFolder}/SDLTetris",
            "cwd": "${workspaceFolder}",
            "valuesFormatting": "parseText"
        },
        
    ]   
}
