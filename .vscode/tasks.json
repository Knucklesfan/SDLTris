{
    "version": "2.0.0",
    "tasks": [{
        "label": "make",
        "command": "make", // Could be any other shell command
        "args": ["-j`nproc`"],
        "type": "shell",
        "options": {
            "cwd": "${workspaceFolder}"
        },
    }]
}
